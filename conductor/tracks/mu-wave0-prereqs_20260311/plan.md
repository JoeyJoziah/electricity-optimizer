# Implementation Plan: Wave 0 — Multi-Utility Pre-requisites

**Track ID:** mu-wave0-prereqs_20260311
**Spec:** spec.md
**Created:** 2026-03-11
**Status:** [x] Complete (2026-03-11)
**Execution Mode:** Loki RARV Cycles (Autonomous)
**Design Doc:** docs/plans/2026-03-11-multi-utility-expansion.md

---

## Overview

Address critical pre-requisites before multi-utility expansion: NREL API domain migration, cache table retention policies, and Neon storage audit. These are blockers — failure to complete before Wave 1 risks service disruption (NREL deadline April 30 2026) and storage overflow (Neon 512 MiB).

---

## Phase 1: NREL API Domain Migration (MU-000a)

### Tasks

- [x] Task 1.1: Audit all NREL API references in codebase
  - Search for `developer.nrel.gov` across all files
  - Document each usage location (file, line, purpose)
  - Check environment variables, config files, and hardcoded URLs

- [x] Task 1.2: Write tests for NREL API endpoints
  - Test current endpoints return expected data with existing domain
  - Test domain migration doesn't break response parsing
  - Mock-based tests for rate limiter behavior

- [x] Task 1.3: Update NREL domain references
  - Replace `developer.nrel.gov` with `developer.nlr.gov` in all locations
  - Update environment variables in `.env.example`, Render dashboard, 1Password
  - Add domain as configurable env var `NREL_API_BASE_URL` for future flexibility

- [x] Task 1.4: Verify NREL migration
  - Run affected test suites
  - Manual smoke test against live API (if accessible)
  - Verify Render deployment picks up new domain

### Verification
- [x]Zero references to `developer.nrel.gov` remain in codebase
- [x]All NREL-dependent tests pass
- [x]`NREL_API_BASE_URL` env var documented

---

## Phase 2: Cache Table Retention Policies (MU-000b)

### Tasks

- [x] Task 2.1: Audit current cache table sizes
  - Query Neon for row counts and estimated sizes of: `weather_cache`, `scraped_rates`, `market_intelligence`, `rate_alerts` (history), any other temporal data
  - Document current Neon storage usage (total and per-table)

- [x] Task 2.2: Write retention cleanup functions
  - Create `backend/services/db_maintenance.py` cleanup functions (or extend existing):
    - `cleanup_weather_cache(days=30)` — DELETE WHERE created_at < NOW() - INTERVAL '30 days'
    - `cleanup_scraped_rates(days=90)` — DELETE WHERE scraped_at < NOW() - INTERVAL '90 days'
    - `cleanup_market_intelligence(days=180)` — DELETE WHERE created_at < NOW() - INTERVAL '180 days'
  - Each function returns count of deleted rows for logging

- [x] Task 2.3: Write tests for retention functions
  - Test each cleanup function removes correct rows
  - Test boundary conditions (exactly 30/90/180 days)
  - Test no accidental deletion of recent data

- [x] Task 2.4: Wire retention into db-maintenance cron
  - Add retention cleanup to `db-maintenance.yml` weekly cron (Sunday 3am UTC)
  - Call cleanup functions after existing VACUUM/ANALYZE steps
  - Log cleanup results to application logs
  - Add Slack notification on cleanup completion (row counts)

- [x] Task 2.5: Add Neon storage monitoring alert
  - Add storage check to `db-maintenance.yml` or separate lightweight cron
  - Alert to Slack #incidents if storage > 400 MiB
  - Document current usage baseline

### Verification
- [x]Retention functions exist with tests
- [x]db-maintenance workflow includes retention cleanup
- [x]Slack alert configured for 400 MiB threshold
- [x]Current Neon storage documented (baseline for Wave 1)

---

## Phase 3: Final Validation

### Tasks

- [x] Task 3.1: Run full backend test suite
- [x] Task 3.2: Run full frontend test suite
- [x] Task 3.3: Verify db-maintenance workflow syntax (act --dry-run)
- [x] Task 3.4: Document Wave 0 completion in CONTINUITY.md

### Verification
- [x]All tests pass (backend + frontend)
- [x]NREL endpoints migrated
- [x]Cache retention policies active
- [x]Neon storage < 400 MiB confirmed
- [x]Wave 1 unblocked

---

## Execution Protocol

Per-phase Loki RARV cycle. Human checkpoint after Phase 2 (before committing cron changes). Memory persistence under namespace `mu-wave0`.

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
