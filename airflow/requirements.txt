# Airflow Requirements for Electricity Optimizer
# Pin versions for reproducibility

# Core Airflow
apache-airflow==2.8.0
apache-airflow-providers-postgres==5.10.0
apache-airflow-providers-redis==3.6.0
apache-airflow-providers-http==4.10.0
apache-airflow-providers-slack==8.6.0

# Database drivers
psycopg2-binary==2.9.9
asyncpg==0.29.0
redis==5.0.1

# HTTP clients
httpx==0.26.0
aiohttp==3.9.1

# Data processing
pandas==2.1.4
numpy==1.26.2
pyarrow==14.0.2

# ML dependencies (for training DAG)
torch==2.1.2
scikit-learn==1.3.2
xgboost==2.0.3
lightgbm==4.2.0

# Utilities
pyyaml==6.0.1
python-dateutil==2.8.2
pytz==2023.3.post1

# Testing
pytest==7.4.3
pytest-mock==3.12.0

# Monitoring
prometheus-client==0.19.0
statsd==4.0.1

# Sentry (error tracking)
sentry-sdk==1.38.0
