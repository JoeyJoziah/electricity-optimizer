# Production Requirements (Free Tier Optimized)
# Minimal dependencies to reduce memory footprint and startup time

# FastAPI and ASGI Server
fastapi>=0.115.0,<1.0
uvicorn[standard]==0.41.0
pydantic==2.12.5
pydantic-settings==2.13.1

# Database
asyncpg==0.31.0
sqlalchemy[asyncio]==2.0.48

# Redis (lighter hiredis parser)
redis[hiredis]==5.0.1

# HTTP Client
httpx>=0.24,<0.29

# Authentication & Security
PyJWT[crypto]>=2.8,<3.0
python-multipart>=0.0.18

# Environment & Configuration
python-dotenv==1.2.2

# Data Validation
email-validator==2.3.0

# Monitoring & Logging (minimal)
prometheus-client==0.24.1
structlog==24.1.0

# Optional: Sentry (only if SENTRY_DSN is set)
# Commented out to save memory - uncomment if needed
# sentry-sdk[fastapi]==1.39.2

# ML Dependencies (only if doing prediction inference)
numpy==1.26.3

# Note: Removed unused dependencies:
# - celery (not used, no background tasks)
# - supabase (fully removed, using Neon Auth / Better Auth)
# - aiohttp (not needed if only using httpx)
# - pandas, scikit-learn (not needed unless training models)
# - alembic (migrations run separately, not at runtime)
# - slowapi (using custom rate limiter)
