# Machine Learning Dependencies for Electricity Price Forecasting
# This is the canonical ML requirements file (requirements-ml.txt was consolidated into this)

# Deep Learning
# TF 2.17.1 is the lowest stable release with Python 3.12 wheels. 2.15 had no
# 3.12 wheels and broke ML Tests CI (ci-ml-tensorflow-py312_20260515).
# TF 2.17 bundles Keras 3 by default; the tf-keras shim keeps the legacy
# tf.keras (Keras 2) API working for price_forecaster.py and hyperparameter
# tuning code. Set TF_USE_LEGACY_KERAS=1 to opt into the shim at runtime.
tensorflow==2.17.1
tf-keras==2.17.0
tensorboard==2.17.1

# ML Frameworks
scikit-learn==1.4.0
xgboost==2.0.3
lightgbm==4.3.0

# Hyperparameter Optimization
optuna==3.5.0

# Optimization
pulp==2.7.0
scipy==1.12.0

# Data Processing
numpy==1.26.0
# pandas 2.1.0 has no cp312 wheels on PyPI; 2.1.1 is the lowest patch with
# Python 3.12 wheels (ci-pandas-py312_20260515).
pandas==2.1.1
holidays==0.40

# Time Series
statsmodels==0.14.0
prophet==1.1.5

# Feature Engineering
category-encoders==2.6.0

# Model Serialization
joblib==1.3.0
# pickle5 removed — protocol 5 is built into Python 3.8+

# Visualization
matplotlib==3.8.0
seaborn==0.13.0
plotly==5.18.0

# Monitoring
mlflow==2.9.0
wandb==0.16.0

# Testing
pytest==7.4.0
pytest-cov==4.1.0

# Utilities
pyyaml==6.0.1
python-dotenv==1.0.0
tqdm==4.66.0
